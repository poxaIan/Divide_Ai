import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  emptyText: {
    fontSize: 16,
    color: '#888',
    marginVertical: 20,
    textAlign: 'center',
  },
  despesaItem: {
    backgroundColor: '#f0f0f0',
    padding: 12,
    borderRadius: 8,
    marginBottom: 8,
  },
});
